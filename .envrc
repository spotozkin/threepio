# Load secrets from .env (NOT committed)
dotenv .env

# Runtime defaults (safe)
export PROVIDER_TTS=${PROVIDER_TTS:-elevenlabs}
export AUDIO_OUTPUT_MODE=${AUDIO_OUTPUT_MODE:-afplay}

# Optional: defaults you can override in .env
export ELEVENLABS_OUTPUT_FORMAT=${ELEVENLABS_OUTPUT_FORMAT:-mp3_44100_128}
